# Benchmarks

This folder is for small tests used before expensive training runs.

Examples:

- GPU matrix speed
- attention implementation comparison
- BF16 vs FP16 vs FP32
- optimizer tests
- batch-size tests
- VRAM use
- tokens per second

The goal is to measure first and avoid wasting long training runs.
