# 01 — Foundations

The first goal was to understand what training means before using a full Transformer.

This stage covered:

- a simple model with a weight and bias
- loss
- gradients
- gradient descent
- a tiny automatic-gradient engine
- a small neural network

Main idea:

`prediction → error → gradient → parameter update`
