# 06 — Data

The project moved from Tiny Shakespeare to FineWeb-Edu.

Dataset growth:

- v1: about 100M characters
- v2: 1B characters
- v3: 3B characters

Large dataset files are not stored in Git.

The repository should only contain the scripts and small metadata needed to rebuild them.
