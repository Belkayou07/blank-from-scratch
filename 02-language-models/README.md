# 02 — Language Models

This stage moved from general neural networks to text prediction.

Models started by predicting the next character from very little context.

Then more context and embeddings were added.

Main idea:

`previous tokens → predict the next token`
